{
  "name": "trustbase",
  "version": "0.0.1",
  "main": "src/index.js",
  "files": [
    "src",
    "build"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=8"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "git add"
    ]
  },
  "scripts": {
    "precommit": "lint-staged",
    "migrate:rinkeby": "truffle migrate --network rinkeby",
    "prepublish": "npm run migrate:rinkeby"
  },
  "dependencies": {
    "ethereumjs-wallet": "^0.6.0",
    "web3": "1.0.0-beta.20",
    "web3-provider-engine": "^13.2.9"
  },
  "devDependencies": {
    "eslint": "^4.6.0",
    "eslint-config-airbnb-base": "^12.0.0",
    "eslint-plugin-import": "^2.7.0",
    "husky": "^0.14.3",
    "lint-staged": "^4.1.3",
    "truffle-hdwallet-provider": "^0.0.3"
  }
}
